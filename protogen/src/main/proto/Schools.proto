syntax = "proto3";

package com.realcloud.campus.proto;

message School {
  int64 id = 1;
  string name = 2;
}

message Schools {
  int32 status = 1;
  string response = 2;
  repeated School schools = 3;
  int32 flag = 4; // 查询结果flag, 1~输入查询,2~定位查询
}